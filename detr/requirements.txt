# cython  <-- 手動で入れたのでコメントアウトまたは削除でOK
# git+https://github.com/cocodataset/cocoapi.git#subdirectory=PythonAPI&egg=pycocotools <-- ビルドが難しいので削除
submitit
# torch>=1.5.0      <-- RunPodのプリインストールを使うのでコメントアウト
# torchvision>=0.6.0 <-- 同上
git+https://github.com/cocodataset/panopticapi.git#egg=panopticapi
scipy
onnx
onnxruntime
pycocotools>=2.0.7  # <-- 修正: 最新のバイナリ版を指定（ビルド不要で高速）
timm                # <-- 追加: DETRのバックボーンでよく必要になります
